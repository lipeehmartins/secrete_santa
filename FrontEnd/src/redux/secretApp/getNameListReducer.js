const nameState = {
    loading: false,
    data: []
};

const getNameListReducer = (state = nameState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default getNameListReducer;